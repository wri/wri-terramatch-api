type: object
properties:
  data:
    type: array
    items:
      $ref: './_index.yml#/TreeSpeciesRead'
  links:
    type: object
    properties:
      first:
        type: string
      last:
        type: string
      prev:
        type: string
      next:
        type: string
  meta:
    type: object
    properties:
      current_page:
        type: integer
      from:
        type: integer
      last_page:
        type: integer
      next:
        type: integer
      unfiltered_total:
        type: integer
