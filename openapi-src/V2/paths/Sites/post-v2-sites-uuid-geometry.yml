summary: Upload bulk geometry to a specific site.
operationId: post-v2-sites-uuid-geometry
tags:
  - V2 Sites
deprecated: true
description: Use POST /api/v2/geometry instead (and include the site ID in the polygon properties)
parameters:
  - type: string
    name: UUID
    in: path
    required: true
  - in: body
    name: body
    schema:
      type: object
      properties:
        geometries:
          type: array
          items:
            $ref: '../../definitions/_index.yml#/GeoJSON'
responses:
  '201':
    description: Created
    schema:
      $ref: '../../definitions/_index.yml#/GeometryPost'
