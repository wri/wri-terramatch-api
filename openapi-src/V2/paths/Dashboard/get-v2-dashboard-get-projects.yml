summary: Retrieve all project.
description: |
  This endpoint returns all project location.
parameters:
  - in: query
    name: country
    type: string
    description: Filter counts and metrics by country.
responses:
  '200':
    description: Successful response
    content:
      application/json:
        schema:
          $ref: '../../definitions/_index.yml#/DashboardGetProjectsResponse'
  '400':
    description: Bad request
  '500':
    description: Internal server error