operationId: get-v2-fproject-pipeline
summary: Get all projects from the pipeline
tags: 
  - V2 Project Pipeline
parameters:
  - in: query
    name: country
    type: string
    description: Optional. Filter counts and metrics by country.
responses:
  '200':
    description: OK
    schema:
      type: array
      items:
        $ref: '../../definitions/_index.yml#/ProjectPipeline'