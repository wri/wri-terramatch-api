summary: Run validation for all polygons in a site
parameters:
  - in: query
    name: uuid
    required: true
    description: The UUID of the polygon
    type: string
responses:
  "200":
    description: Successful response
    schema:
      $ref: '../../definitions/_index.yml#/DelayedJobsResponse'